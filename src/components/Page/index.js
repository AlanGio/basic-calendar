import React from 'react';
import Calendar from '../Calendar/Calendar';
import './Page.scss';

const Page = () => (
  <div className="Page">
    <main>
      <Calendar />
    </main>
  </div>
);

export default Page;
